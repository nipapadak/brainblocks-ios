# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BrainBlocks' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BrainBlocks
  pod "QRCode"
  pod 'Alamofire', '~> 4.5'
  pod 'BrainBlocksKit', :git => 'https://github.com/brainblocks/brainblocks-ios-sdk.git', :tag => '1.0'

  target 'BrainBlocksTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BrainBlocksUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
